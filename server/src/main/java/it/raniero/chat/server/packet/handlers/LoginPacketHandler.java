package it.raniero.chat.server.packet.handlers;

import it.raniero.chat.api.server.packet.IPacket;
import it.raniero.chat.api.server.packet.IPacketHandler;
import it.raniero.chat.api.user.connection.ISocketWrapper;

public class LoginPacketHandler implements IPacketHandler {


    @Override
    public void handle(ISocketWrapper wrapper, IPacket packet) {

    }
}
